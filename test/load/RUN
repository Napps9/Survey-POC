# Parameters for the next Actions-driven load-test run (see load_test.yml).
# Pushing a change to this file fires the workflow from this branch.
# Run 17: baseline on the NEW single big box — scratch web resized to 8c-16g
# with WEB_CONCURRENCY=1, RAILS_MAX_THREADS=24 (ONE Puma process, 24 threads),
# page cache + Key Value live. Testing the thesis that with the play page
# cached the journey is now I/O-bound, so one process spreads across the cores
# via the GVL-released threads — no worker extraction needed. 40/s over 2
# generators (20/s each, under one runner's ~185 req/s ceiling) as the health
# baseline before escalating to 80 and 125.
BASE_URL=https://vertonow.onrender.com
TOKEN=5rJks8kFiOaNOWeTmzkcinYf
SCRIPT=test/load/journey.js
ARRIVALS_PER_S=40
RAMP_S=60
HOLD_S=240
DWELL_SCALE=0.05
BROWNOUT=0
GENERATORS=2
