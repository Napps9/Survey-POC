# Parameters for the next Actions-driven load-test run (see load_test.yml).
# Pushing a change to this file fires the workflow from this branch.
# Run 23: run 22 with SKIP=images — same two-box bucket-backed fleet, same
# 80/s, but the journey no longer loads the logo + 6 card images. Run 22
# (branded) served ~1,120 req/s with the images flawless (141k loads, 100%
# 200, med 251 ms) but the journey endpoints no better than one box (submit
# med 2.7 s, p95 5.3 s; p99 29 s). Either the second instance was not yet in
# rotation (fired ~2 min after the scale-up) or the wall has moved to a
# shared tier — the database, or the ~670 Rails 302s/s the images cost.
# This run isolates the image cost and previews the fix (presigned bucket
# URLs baked into the cached play page, zero Rails requests per image).
# Clean here → images were the cost → ship the presign change and re-run
# branded. Still slow → shared wall (database) → look there.
BASE_URL=https://vertonow.onrender.com
TOKEN=5rJks8kFiOaNOWeTmzkcinYf
SCRIPT=test/load/journey.js
ARRIVALS_PER_S=80
RAMP_S=60
HOLD_S=240
DWELL_SCALE=0.05
BROWNOUT=0
GENERATORS=4
SKIP=images
