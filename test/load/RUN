# Parameters for the next Actions-driven load-test run (see load_test.yml).
# Pushing a change to this file fires the workflow from this branch.
# Run 26: THE RE-BASELINE. Branded deck, 80/s over 4 generators, two 12c-24g
# boxes — the run-24 shape — but the FIRST run against a Key Value store that
# is not saturated. Runs 19-24 all ran against the free tier (0.05 CPU, 25 MB,
# 50 connections), which Render's graphs show pinned at 100% CPU throughout,
# peaking near 40 of its 50 connections. Rails.cache fails OPEN, so that was
# silent: the page cache missed, every request re-rendered, and rate_limit
# paid up to its 0.5s read timeout per call. Both instances are now 1 GB /
# 1,000 connections with Persistence Mode OFF (Journal + Snapshot was
# fsyncing every counter write to disk).
#
# So every previous measurement of the web tier was taken through a degraded
# cache and is suspect — including "one 12-core box tops out around 2s".
# Expect: journey medians well under 1s and the thresholds to pass. If they
# do, go to the 125/s proof. If the journey endpoints are STILL ~2s with a
# healthy store, the wall is the database and nothing else is left.
BASE_URL=https://vertonow.onrender.com
TOKEN=5rJks8kFiOaNOWeTmzkcinYf
SCRIPT=test/load/journey.js
ARRIVALS_PER_S=80
RAMP_S=60
HOLD_S=240
DWELL_SCALE=0.05
BROWNOUT=0
GENERATORS=4
