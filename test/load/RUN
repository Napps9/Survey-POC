# Parameters for the next Actions-driven load-test run (see load_test.yml).
# Pushing a change to this file fires the workflow from this branch.
# Run 20: the DB-vs-web discriminator. IDENTICAL load to run 19 (80/s, 4
# generators, ramp 60 / hold 240 / dwell 0.05) with the ONE variable changed:
# the scratch Postgres was resized up one tier (4c-16g -> 8c-32g) while the
# web box is held at 8c-16g cluster (WEB_CONCURRENCY=8, RAILS_MAX_THREADS=5),
# page cache + Key Value, same commit 7452994. Run 19 saturated at 80/s
# (~388 req/s, 3.8s median, 0 errors). If run 20 comes back clean, the DB was
# the wall -> Branch A (one box + bigger DB) and on to the 125/s proof. If it
# still saturates, the wall is the web box -> Branch B (bigger single box).
BASE_URL=https://vertonow.onrender.com
TOKEN=5rJks8kFiOaNOWeTmzkcinYf
SCRIPT=test/load/journey.js
ARRIVALS_PER_S=80
RAMP_S=60
HOLD_S=240
DWELL_SCALE=0.05
BROWNOUT=0
GENERATORS=4
